{
  "name": "scuttle-server",
  "version": "1.0.0",
  "dependencies": {
    "bower": "~1.3.0"
  },
  "engine": {
    "node": "0.10.x",
    "npm": "~1.3.0"
  },
  "scripts": {
    "postinstall": "bundle exec rake bower:install || echo \"Please run `bundle exec rake bower:install` when your ruby environment is set up.\""
  }
}
